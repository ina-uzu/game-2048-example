apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: game-2048
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: game-2048
              servicePort: 8080
              path: /
              pathType: Prefix